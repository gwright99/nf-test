manifest {
    name = "Rob Syme test template"
}

params {
    processCount = 100
    layerSize = '10,100'
}

process.container = 'quay.io/nextflow/bash'

profiles {
    docker {
        docker.enabled = true
    }
}