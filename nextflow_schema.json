{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/Rob Syme test template//nextflow_schema.json",
    "title": "Rob Syme test template pipeline parameters",
    "description": "",
    "type": "object",
    "$defs": {
        "basic_inputs": {
            "title": "Basic inputs",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "processCount": {
                    "type": "integer",
                    "default": 100,
                    "description": "The number of parallel processes to trigger"
                }
            },
            "required": [
                "processCount"
            ]
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/basic_inputs"
        }
    ]
}